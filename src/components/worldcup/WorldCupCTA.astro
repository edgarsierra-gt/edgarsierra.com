<section class="worldcup-cta" data-reveal>
  <div>
    <p class="eyebrow">Por Edgar Sierra</p>
    <h2>Estadística aplicada, en público</h2>
    <p>
      Este proyecto es parte de mi laboratorio de estadística aplicada. También escribo sobre IA,
      datos, marketing analytics y tecnología aplicada a problemas reales de Guatemala y Centroamérica.
    </p>
  </div>
  <nav aria-label="Siguientes pasos del Laboratorio Mundial 2026">
    <a href="/mundial-2026/metodologia">Ver metodología</a>
    <a href="https://github.com/edgarsierra-gt/mundial-2026-predictor" target="_blank" rel="noopener noreferrer">Ver GitHub</a>
    <a href="/newsletter">Newsletter</a>
    <a href="/proyectos">Otros proyectos</a>
  </nav>
</section>

<style>
  .worldcup-cta {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: var(--space-4);
    align-items: center;
    padding: var(--space-4);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    background: var(--surface);
  }

  .eyebrow {
    font-family: var(--font-ui);
    font-size: var(--text-sm);
    color: var(--accent);
    font-weight: 600;
    margin-bottom: var(--space-1);
  }

  h2 {
    font-size: var(--text-2xl);
    margin-bottom: var(--space-2);
  }

  p {
    color: var(--ink-muted);
    max-width: var(--reading);
  }

  nav {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
    justify-content: flex-end;
  }

  nav a {
    font-family: var(--font-ui);
    font-size: var(--text-sm);
    font-weight: 600;
    color: var(--accent);
    text-decoration: none;
    padding: 0.55rem var(--space-2);
    border: 1px solid var(--border);
    border-radius: var(--radius);
  }

  nav a:hover {
    text-decoration: underline;
  }

  @media (max-width: 760px) {
    .worldcup-cta {
      grid-template-columns: 1fr;
    }

    nav {
      justify-content: flex-start;
    }
  }
</style>

