<aside class="uncertainty" data-reveal>
  <h2>Cómo leer estas predicciones</h2>
  <p>
    Estas predicciones son probabilidades, no certezas. Un equipo con 70% de
    probabilidad todavía puede perder. El marcador más probable individual suele
    tener una probabilidad baja y se muestra como referencia, no como promesa.
  </p>
</aside>

<style>
  .uncertainty {
    padding: var(--space-4);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    background: var(--code-bg);
  }

  h2 {
    font-size: var(--text-xl);
    margin-bottom: var(--space-2);
  }

  p {
    color: var(--ink-muted);
    max-width: var(--reading);
  }
</style>

