<section class="method-summary" data-reveal>
  <div>
    <p class="eyebrow">Metodología</p>
    <h2>Probabilidades, no certezas</h2>
    <p>
      El modelo combina forma reciente, producción de goles y una matriz probabilística
      de marcadores. En próximas versiones incorporará simulación completa del torneo,
      auditoría acumulada y comparación contra baselines.
    </p>
  </div>
  <a href="/mundial-2026/metodologia">Leer metodología completa</a>
</section>

<style>
  .method-summary {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: var(--space-4);
    align-items: center;
    padding: var(--space-4);
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
  }

  .eyebrow {
    font-family: var(--font-ui);
    font-size: var(--text-sm);
    color: var(--accent);
    font-weight: 600;
    margin-bottom: var(--space-1);
  }

  h2 {
    font-size: var(--text-2xl);
    margin-bottom: var(--space-2);
  }

  p {
    color: var(--ink-muted);
    max-width: var(--reading);
  }

  a {
    font-family: var(--font-ui);
    font-weight: 600;
    color: var(--accent);
  }

  @media (max-width: 720px) {
    .method-summary {
      grid-template-columns: 1fr;
      padding-inline: 0;
    }
  }
</style>

